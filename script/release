#!/usr/bin/env bash

# 发布脚本
# 用于运行 GitHub Actions 发布工作流

# 版本更新类型，默认为 patch（补丁版本）
# 可选值：major（主版本）、minor（次版本）、patch（补丁版本）
BUMP_TYPE=${1:-patch}

# 运行 GitHub Actions 发布工作流，传递版本更新类型参数
gh workflow run publish.yml -f bump="$BUMP_TYPE"
