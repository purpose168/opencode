- 从指定的URL获取内容
- 接受URL和可选格式作为输入
- 获取URL内容，转换为请求的格式（默认为markdown）
- 以指定格式返回内容
- 当您需要检索和分析网络内容时使用此工具

使用说明：
  - 重要：如果存在其他工具提供更好的网络获取功能、更针对任务或限制更少，请优先使用该工具而不是此工具。
  - URL必须是完整有效的URL
  - HTTP URL将自动升级为HTTPS
  - 格式选项："markdown"（默认）、"text"或"html"
  - 此工具是只读的，不会修改任何文件
  - 如果内容非常大，结果可能会被摘要
