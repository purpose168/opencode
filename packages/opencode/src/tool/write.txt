将文件写入本地文件系统。

使用说明：
- 如果提供的路径上已存在文件，此工具将覆盖现有文件。
- 如果是现有文件，您必须先使用Read工具读取文件内容。如果您未先读取文件，此工具将失败。
- 始终优先编辑代码库中的现有文件。除非明确要求，否则绝不写入新文件。
- 绝不主动创建文档文件（*.md）或README文件。只有在用户明确要求时才创建文档文件。
- 只有在用户明确要求时才使用表情符号。除非被要求，否则避免在文件中写入表情符号。
