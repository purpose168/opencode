# JavaScript

安装依赖：

```bash
# 使用 bun 包管理器安装项目依赖
bun install
```

运行：

```bash
# 使用 bun 运行 TypeScript 入口文件
bun run index.ts
```

本项目使用 bun v1.2.12 的 `bun init` 创建。[Bun](https://bun.sh) 是一个快速的全功能 JavaScript 运行时。
