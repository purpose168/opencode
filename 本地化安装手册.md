# OpenCode 本地化安装手册

## 项目概述

OpenCode 是一个 AI 驱动的开发工具，提供了丰富的 CLI 命令和功能，支持与多种 AI 服务提供商集成。

- **项目类型**：Node.js 项目，使用 Bun 作为运行时和包管理器
- **开发语言**：TypeScript
- **项目结构**：Monorepo，包含多个包
- **主要功能**：AI 辅助开发、代码生成、模型管理等

## 环境准备

### 系统要求

- **操作系统**：Linux、macOS、Windows
- **架构**：x64 或 arm64
- **内存**：建议至少 4GB RAM
- **磁盘空间**：至少 1GB 可用空间

### 依赖安装

#### 1. 安装 Bun 运行时

OpenCode 使用 Bun 作为主要运行时和包管理器，必须先安装 Bun。

**Linux/macOS 安装命令**：

```bash
curl -fsSL https://bun.sh/install | bash
```

**Windows 安装命令**：

```powershell
powershell -c "irm bun.sh/install.ps1 | iex"
```

#### 2. 验证 Bun 安装

```bash
bun --version
```

应显示 Bun 版本信息，如 `1.3.5` 或更高版本。

## 项目安装

### 1. 克隆项目（如果尚未克隆）

```bash
git clone https://github.com/sst/opencode.git
cd opencode
```

### 2. 安装项目依赖

在项目根目录执行：

```bash
bun install
```

这将安装所有工作区包的依赖项。

## 配置设置

### 环境变量

OpenCode 可能需要以下环境变量（根据使用的功能）：

- **AI 服务提供商 API 密钥**：如 `OPENAI_API_KEY`、`ANTHROPIC_API_KEY` 等
- **GitHub 认证信息**：如 `GITHUB_TOKEN`（用于 GitHub 相关功能）

可以在执行命令时设置这些环境变量，或在 `.env` 文件中配置。

### 配置文件

项目使用 TypeScript 配置文件 `tsconfig.json`，通常不需要手动修改。

## 启动命令

### 开发模式

在项目根目录启动开发模式：

```bash
bun run dev
```

这将运行 `packages/opencode` 包中的 `src/index.ts` 文件。

### 构建项目

构建生产版本：

```bash
# 在 packages/opencode 目录中执行
bun run build

# 或从根目录执行
bun run --cwd packages/opencode build
```

构建过程会为不同平台和架构生成可执行文件。

### 运行 CLI 命令

OpenCode 提供了多种 CLI 命令，基本使用方式：

```bash
# 从项目根目录执行
bun run --cwd packages/opencode src/index.ts [command] [options]

# 或使用构建后的可执行文件
./packages/opencode/dist/[platform]/bin/opencode [command] [options]
```

## 可用命令

OpenCode 提供了丰富的 CLI 命令，包括：

- **run**：运行代码
- **generate**：生成代码
- **auth**：认证相关操作
- **agent**：AI 代理操作
- **upgrade**：升级 OpenCode
- **models**：管理 AI 模型
- **serve**：启动服务
- **web**：Web 相关操作
- **github**：GitHub 相关操作
- **tui**：终端用户界面操作

要查看所有可用命令和选项：

```bash
bun run --cwd packages/opencode src/index.ts --help
```

## 项目结构

### 主要目录

- **packages/opencode**：主要包，包含 CLI 工具实现
- **packages/sdk**：SDK 实现
- **packages/console**：控制台相关功能
- **packages/slack**：Slack 集成

### 核心文件

- **packages/opencode/src/index.ts**：CLI 入口文件
- **packages/opencode/src/cli/**：CLI 命令实现
- **packages/opencode/script/build.ts**：构建脚本

## 故障排除

### 常见问题

1. **Bun 安装失败**
   - 确保网络连接正常
   - 尝试使用管理员权限安装
   - 检查系统是否满足 Bun 的要求

2. **依赖安装失败**
   - 确保 Bun 版本正确
   - 尝试清理缓存后重新安装：`bun install --clean`

3. **构建失败**
   - 检查 TypeScript 类型错误：`bun run typecheck`
   - 确保所有依赖项已正确安装

4. **运行命令失败**
   - 检查命令语法是否正确
   - 确保相关服务的 API 密钥已配置
   - 查看日志文件获取详细错误信息

### 日志查看

OpenCode 会生成日志文件，可通过以下命令查看日志位置：

```bash
bun run --cwd packages/opencode src/index.ts [command] --print-logs
```

## 开发工作流

### 代码修改

1. 修改代码后，运行类型检查：
   ```bash
   bun run typecheck
   ```

2. 运行测试：
   ```bash
   bun run --cwd packages/opencode test
   ```

3. 构建并测试：
   ```bash
   bun run --cwd packages/opencode build
   ```

### 提交代码

项目使用 Husky 管理 Git hooks，确保代码质量。提交前会自动运行相关检查。

## 总结

OpenCode 是一个功能强大的 AI 驱动开发工具，通过以上步骤可以在本地环境成功部署和运行。主要关键点：

1. 安装 Bun 运行时
2. 安装项目依赖
3. 配置必要的环境变量
4. 使用相应的命令启动和构建项目

如需更多帮助，请参考项目文档或 GitHub 仓库。

---

**更新时间**：2026-01-31
**版本**：1.0.0
**适用项目**：OpenCode
